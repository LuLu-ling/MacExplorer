namespace MacExplorer.Controls;

public static class SectionMotion
{
    public static readonly TimeSpan Move = TimeSpan.FromMilliseconds(220);
    public static readonly TimeSpan Fade = TimeSpan.FromMilliseconds(180);
}
